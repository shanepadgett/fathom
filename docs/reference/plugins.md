# Bundled plugins

Generated by `deno task docs` from each plugin's backend definition: the
tokens it requires and provides, and its configuration schema.

## llm

- Requires: `fathom.credentials`, `fathom.api`
- Provides: `fathom.llm`, `fathom.llm.providers`

```json
{
  "type": "object",
  "properties": {}
}
```

## provider-xai

- Requires: `fathom.llm.providers`, `fathom.credentials.logins`
- Provides: none

```json
{
  "type": "object",
  "properties": {}
}
```

## provider-anthropic

- Requires: `fathom.llm.providers`, `fathom.credentials.logins`
- Provides: none

```json
{
  "type": "object",
  "properties": {}
}
```

## provider-openai

- Requires: `fathom.llm.providers`, `fathom.credentials.logins`
- Provides: none

```json
{
  "type": "object",
  "properties": {}
}
```

## storage

- Requires: `fathom.environment`
- Provides: `fathom.storage`

```json
{
  "type": "object",
  "properties": {}
}
```

## credentials

- Requires: `fathom.environment`, `fathom.api`
- Provides: `fathom.credentials`, `fathom.credentials.logins`

```json
{
  "type": "object",
  "properties": {}
}
```
