# Fathom

Deno monorepo for the Fathom agent runtime and desktop application.

```text
apps/desktop/       Installable desktop host
packages/runtime/   Host-independent agent runtime
packages/ai/        Model, provider, and credential services built on Pi AI
```

Run repository checks with `mise exec -- deno task check`.
