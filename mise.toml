[tools]
deno = "2.9.7"
python = "3.14"

[tasks.design]
description = "Open the local design system"
dir = "{{config_root}}/design"
run = "deno task dev --open"

[tasks.dev]
description = "Build the UI and open Fathom in its native window with hot reload"
run = "deno task dev"

[tasks."dev:browser"]
description = "Build the UI and run Fathom in a browser tab for DevTools"
run = "deno task dev:browser"

[tasks."build:ui"]
description = "Build the app shell and plugin UI bundles"
run = "deno task build:ui"

[tasks."desktop:build"]
description = "Package the native app as dist/Fathom.app"
run = "deno task desktop:build"

[tasks.new]
description = "Scaffold a plugin under plugins/"
run = "deno task new"

[tasks."plugin:add"]
description = "Register a plugin outside the repo; starts disabled"
run = "deno task plugin:add"

[tasks."plugin:remove"]
description = "Unregister a plugin outside the repo"
run = "deno task plugin:remove"

[tasks.fmt]
description = "Format source and project configuration with Oxfmt"
run = "deno run -A oxfmt --write ."

[tasks."fmt:check"]
description = "Check formatting without changing files"
run = "deno run -A oxfmt --check ."

[tasks.lint]
description = "Lint TypeScript and Solid code with Oxlint"
run = "deno run -A oxlint --deny-warnings apps packages plugins"

[tasks."lint:fix"]
description = "Apply safe Oxlint fixes"
run = "deno run -A oxlint --fix apps packages plugins"

[tasks."lint:deno"]
description = "Check Deno code and JSR public types"
run = "deno lint"

[tasks.typecheck]
description = "Type-check the Deno workspace"
run = "deno task check"

[tasks."publish:check"]
description = "Dry-run the JSR publish of the SDK"
dir = "{{config_root}}/packages/sdk"
run = "deno publish --dry-run --allow-dirty"

[tasks.check]
description = "Check formatting, lint, and types"
depends = ["fmt:check", "lint", "lint:deno", "typecheck"]
